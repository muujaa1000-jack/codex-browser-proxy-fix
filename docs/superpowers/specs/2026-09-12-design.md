# Browser proxy workaround: approved scope

Build an unofficial Windows / PowerShell 7 utility with Check, Apply and Restore operations. Provide complete English and Chinese instructions. Publish only original source, synthetic tests and documentation under MIT; never include vendor binaries, the vendor launcher, local diagnostics, configuration, credentials, user paths, or personal commit metadata.

Only the verified unified-computer-use 26.903.71938 launcher with SHA256 a50b66879f7b72e45ab6fbaad77eff14a87680a946135f410c121b9b166a2597 is supported initially. Unknown bytes or versions are refused. Detect plugin candidates from the local Codex home, require an explicit selection if ambiguous, and check the generated manifest references that launcher. Do not execute files discovered through the manifest.

Apply requires an explicit credential-free HTTP proxy URL on a literal loopback IP, with an explicit port. Preserve existing process proxy values; supply defaults for HTTP_PROXY, HTTPS_PROXY and ALL_PROXY using that URL, plus localhost exclusions. Probe only the specified local TCP endpoint; do not send web requests or test credentials. A listener alone does not establish that the proxy protocol or upstream access works.

Validate the original fingerprint before transforming a unique environment spread anchor. Write an exclusive byte-for-byte local backup, use atomic replacement, and verify the output. Repeated applications are no-ops only for an exactly matching patch. Restore requires the verified original backup and the exact recognized patched bytes. Refuse intervening edits, reparse-point paths and unsupported content. Never kill processes or change global proxy, login, browser permissions or security checks.

Check is read-only. It reports file compatibility, not proof of process environment inheritance or a diagnosis of every browser timeout. Applications must be restarted manually after a change. Provide real-page acceptance steps and distinguish a previous single-machine successful experiment from cross-version support.

Tests use synthetic launchers in temporary directories and module-local fixture fingerprints; no production command exposes a fingerprint bypass. Include repeated apply, restore, changed files, corrupted backups, invalid URLs, no writes on refusal, ambiguous discovery, and no telemetry. Run an additional unpublished integration test against a temporary copy of the locally verified original; never modify the live installation to test the public tool.
